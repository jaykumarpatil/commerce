server:
  port: ${PORT:8088}

spring:
  application:
    name: admin-service
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://postgres:5432/admindb}
    username: ${DATASOURCE_USERNAME:${POSTGRES_USER}}
    password: ${DATASOURCE_PASSWORD:${POSTGRES_PASSWORD}}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud:
    stream:
      bindings:
        admin-events-out-0:
          destination: admin-events
          content-type: application/json

eureka:
  client:
    enabled: ${ENABLE_EUREKA:false}
    service-url:
      defaultZone: ${EUREKA_URI:http://eureka:8761/eureka}
  instance:
    prefer-ip-address: true
    hostname: ${HOSTNAME:admin-service}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.projects: DEBUG
    org.hibernate.SQL: DEBUG
