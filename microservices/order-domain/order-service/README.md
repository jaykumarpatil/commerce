# Module: microservices/order-domain/order-service

Purpose:
- Domain layer for orders; defines order model and domain services used by the order service.

Notes:
- Used by the microservices/order-service to implement business rules for ordering.
